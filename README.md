# ThreeJS and TypeScript Biolerplate

## How to start the server?

1. Install yarn: `npm install -g yarn`
2. In the project directory, run: `yarn`
3. After installing packages, run: `yarn dev` or `yarn host`
